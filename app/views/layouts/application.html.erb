<!DOCTYPE html>
<html>
  <head>
    <title>Watson: shop safely</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Arvo&family=Golos+Text&family=Krona+One&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= favicon_link_tag "favicon_2.png", rel: "icon", type: "image/png" %>

  </head>

  <body class="<%= "cream-background" if devise_controller? || params[:controller] == "bookmarks"  %>" >

 <div style="text-align: center;">
    <%= link_to searches_path, class: 'logo' do %>
    <%= image_tag "mainlogo.png", width: "100%" %>
    <% end %>
  </div>

    <%= render "shared/flashes" %>
    <%= yield %>
  </body>
</html>
